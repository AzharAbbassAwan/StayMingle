<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"
      ><i class="fa-regular fa-compass"></i
    ></a>
    <div class="navbar-nav nav-search nav-search-small">
      <form class="form-inline d-flex my-2 my-lg-0 needs-validation" method="post" action="/listings/search" novalidate>
        <input
          class="form-control mr-sm-2 search-input"
          type="search"
          name="userSearch"
          placeholder="Search destinations" required
        />
        <button class="btn search-btn" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </form>
    </div>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <div class="navbar-nav ms-auto nav-search">
        <form class="form-inline d-flex my-2 my-lg-0 needs-validation" method="post" action="/listings/search" novalidate>
          <input
            class="form-control mr-sm-2 search-input"
            type="search"
            name="userSearch"
            placeholder="Search destinations" required
          />
          <div class="invalid-feedback">Enter a valid text</div>
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>
      </div>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if(currUser === undefined){ %>
        <a class="nav-link" aria-current="page" href="/signup">Signup</a>
        <a class="nav-link" href="/login">Login</a>
        <% } %> <% if(currUser !== undefined){ %>
        <a class="nav-link" href="/logout">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
